import { listKeyboards } from "./listKeyboards";
import { createKeyboard } from "./createKeyboard";
import { itemKeyboard } from "./itemKeyboard";

const gql = {
  createKeyboard,
  listKeyboards,
  itemKeyboard,
};

export default gql;
